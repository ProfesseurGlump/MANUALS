\section{Exercice}\etchs{2}{5}

Trouver un fichier avec lequel vous travaillez et progresser vers
son milieu. Trouver son nom de tampon (\tm{(buffer-name)}), nom de
fichier (\tm{(buffer-file-name)}), la longueur (\tm{(buffer-size)}), et
votre position (\tm{(point)}) dans le fichier. 

